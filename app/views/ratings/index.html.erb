<h1>Ratings </h1>
<% @ratings.each do |rating| %>
  Rating: <%= show_stars(rating.stars) %>
  User: <%=  rating.user.name %>
  <br>
  <%= link_to "Read the Review", book_rating_path(rating.book, rating) %></li>
<% end %>